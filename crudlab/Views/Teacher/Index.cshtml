@using Entities
@model IEnumerable<Teacher>

<div class="container">
    <div class="row">
        <a asp-controller="Teacher" asp-action="Create" class="btn btn-primary">Add</a>
    </div>
    <div class="row">
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Full Name</th>
                    <th>Experience</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var i in Model)
                {
                    <tr>
                        <td>
                            @i.Id
                        </td>
                        <td>
                            <a asp-action="Get" asp-route-id="@i.Id">@($"{i.Name} {i.Surname}")</a>

                        </td>
                        <td>
                            @i.Experience
                        </td>
                        <td>
                            <a class="btn" asp-action="Update" asp-route-id="@i.Id">Edit</a>
                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@i.Id">Delete</a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>
